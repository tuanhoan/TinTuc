@model IEnumerable<TinTuc.Models.Theloaitin>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Thêm mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tentheloai)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tentheloai)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.Maloai }) |
            @Html.ActionLink("Chi Tiết", "Details", new { id=item.Maloai }) |
            @*@Html.ActionLink("Xóa", "Delete", new { id=item.Maloai })*@
            <button onclick="myFunction(@item.Maloai)">Xóa</button>
        </td>
    </tr>
}

</table>
<script>
    function myFunction(Id) {
        var txt;
        if (confirm("Bạn có muốn xóa " + Id + "Không?")) {
            window.open("delete/"+Id,);
        } else { 
        } 
    }
</script>